# Sherlock Database Seeds

Status: Phase 10 Database Setup completed

No seed data is included in Phase 10.

Future seed files may be added only when useful for local development or demos. They must follow these rules:

- Use fake/demo data only.
- Use example domains such as `example.com` or `.test` domains only.
- Do not include real emails, customer names, target URLs, API keys, tokens, cookies, passwords, private keys, headers, or secrets.
- Do not include real scan outputs, evaluator outputs, report drafts, screenshots, or customer evidence.
- Mark all seed data as demo-only.
- Keep production data out of seed files.
